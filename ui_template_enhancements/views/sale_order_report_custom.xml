<odoo>
  <template
    id="ui_template_enhancements_sale_report_mod"
    inherit_id="sale.report_saleorder_document"
    name="Custom Sale Order Report Enhancements"
    priority="99"
  >
    <!-- Modificar campos adicionales en la dirección -->
    <xpath expr="//t[@t-set='address']/div" position="attributes">
      <attribute
        name="t-options"
      >{'widget': 'contact', 'fields': ['address', 'name', 'phone', 'mobile', 'email'], 'no_marker': True}</attribute>
    </xpath>

    <!-- Actualizar direcciones de facturación y envío -->
    <xpath expr="//t[@t-set='information_block']/div" position="replace">
      <div
        t-field="doc.partner_invoice_id"
        t-options="{'widget': 'contact', 'fields': ['address', 'name', 'phone', 'mobile', 'email'], 'no_marker': True, 'phone_icons': True}"
      />
    </xpath>
    <xpath
      expr="//t[@t-set='information_block']/t[@t-if='doc.partner_shipping_id != doc.partner_invoice_id']"
      position="replace"
    >
      <t t-if="doc.partner_shipping_id != doc.partner_invoice_id">
        <strong>Shipping Address:</strong>
        <div
          t-field="doc.partner_shipping_id"
          t-options="{'widget': 'contact', 'fields': ['address', 'name', 'phone', 'mobile', 'email'], 'no_marker': True, 'phone_icons': True}"
        />
      </t>
    </xpath>

    <!-- Ocultar la columna de impuestos -->
    <xpath expr="//th[@name='th_taxes']" position="attributes">
      <attribute name="class">d-none</attribute>
    </xpath>
    <xpath expr="//td[@name='td_taxes']" position="replace">
      <!-- Eliminado -->
    </xpath>

    <!-- Agregar tabla de opciones de productos -->
    <xpath expr="//div[@name='signature']" position="after">
      <div t-if="doc.sale_order_option_ids and doc.state in ['draft', 'sent']">
        <t
          t-set="has_option_discount"
          t-value="any(option.discount != 0.0 for option in doc.sale_order_option_ids)"
        />
        <h4 name="h_optional_products">
          <span>Options</span>
        </h4>
        <table name="table_optional_products" class="table table-sm">
          <thead>
            <tr>
              <th name="th_option_name" class="text-start">Description</th>
              <th
                t-if="has_option_discount"
                name="th_option_discount"
                groups="product.group_discount_per_so_line"
                class="text-start"
              >Disc.%</th>
              <th name="th_option_price_unit" class="text-end">Unit Price</th>
            </tr>
          </thead>
          <tbody class="sale_tbody">
            <tr t-foreach="doc.sale_order_option_ids" t-as="option">
              <td name="td_option_name">
                <span t-field="option.name" />
              </td>
              <td
                t-if="has_option_discount"
                name="td_option_discount"
                groups="product.group_discount_per_so_line"
              >
                <strong t-if="option.discount != 0.0" class="text-info">
                  <t
                    t-out="((option.discount % 1) and '%s' or '%d') % option.discount"
                  />%
                </strong>
              </td>
              <td name="td_option_price_unit">
                <strong class="text-end">
                  <div
                    t-field="option.price_unit"
                    t-options="{'widget': 'monetary', 'display_currency': doc.pricelist_id.currency_id}"
                    t-att-style="option.discount and 'text-decoration: line-through' or None"
                    t-att-class="option.discount and 'text-danger' or None"
                  />
                  <div t-if="option.discount">
                    <t
                      t-out="((1-option.discount / 100.0) * option.price_unit)"
                      t-options="{'widget': 'monetary', 'display_currency': doc.currency_id}"
                    />
                  </div>
                </strong>
              </td>
            </tr>
          </tbody>
        </table>
      </div>
    </xpath>
  </template>
</odoo>
